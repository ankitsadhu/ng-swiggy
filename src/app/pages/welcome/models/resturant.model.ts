export type resturant = {
    id: string,
    name: string,
    cloudinaryImageId: string,
    locality: string,
    areaName: string,
    cuisines: Array<string>,
    avgRating: number,
    isOpen: boolean
}